Re: it being better practice to destructure the props in the function definition rather than in the body of the code. How does this weigh up? 

        <Screen 
            equation={this.state.equation}
            result={this.state.result}
        />